package com.troy.trade.ws.service.impl;

import org.springframework.stereotype.Component;

@Component
public class UserCheckService {

//    @Autowired
//    private TokenConverter tokenConverter;
//
//    public CheckTokenResData currentUser(String token) throws Exception {
//        CheckTokenResData checkTokenResData = this.tokenConverter.convertAccessToken(token);
//        return checkTokenResData;
//    }

    /**
     * description: 读取访问令牌
     *
     * @param
     * @return
     */
//    public String readAccessToken(HttpServletRequest request) {
//        if (request == null) {
//            return null;
//        }
//        String authorization = request.getHeader(HttpHeaders.AUTHORIZATION);
//        if (StringUtils.isEmpty(authorization)) {
//            return authorization;
//        }
//        String[] typeAndValue = authorization.split(" ");
//        String token = typeAndValue[typeAndValue.length - 1].trim();
//        if (Constants.DEFAULT_EMPTY_TOKEN.equalsIgnoreCase(token)) {
//            return null;
//        }
//        return token;
//    }
}
